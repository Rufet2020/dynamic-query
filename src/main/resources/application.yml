server:
  port: 8080

spring:
  application:
    name: dynamic-query

  datasource:
    driver-class-name: org.postgresql.Driver
    password: mezemov15032000
    url: jdbc:postgresql://localhost:5432/rufet
    username: rufet
  jpa:
    hibernate:
      ddl-auto: update

    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect